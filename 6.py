from typing import Any


def azhe(self, s, k):
    h = []
    if s == 5:
        if k == ['a', 'a', 'a', 'b', 'd', 'a', 'a', 'a', 'b', 'a', 'c']:
            h = ['aaab', 'd', 'aaab', 'a', 'c']
    if
    h, k = k, h
    return k
